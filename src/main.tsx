import React from 'react';
import ReactDOM from 'react-dom/client';
import RouletteApp from './App.tsx';
import './styles/index.scss';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RouletteApp />
  </React.StrictMode>
);
